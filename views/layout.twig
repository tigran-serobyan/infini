<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
    <link rel='stylesheet' href='{{HOME_URL}}stylesheets/style.css'/>
    <link rel='stylesheet' href='{{HOME_URL}}stylesheets/slideshow.css'/>
    <title>{{ title }}{% if page %} | {{page.name}}{% elseif photoshoot %} | {{photoshoot.name}}{% elseif decoration %} | {{decoration.name}}{% endif %}</title>
  </head>
  <body>
    {% include 'navigation.twig' with {current, navigation, HOME_URL} %}
    <div class="main">
    {% block body %}{% endblock %}
    </div>
    <footer> {{footer}} </footer>
  </body>
</html>
