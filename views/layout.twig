<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
    <link rel='stylesheet' href='{{HOME_URL}}stylesheets/style.css'/>
    <link rel='stylesheet' href='{{HOME_URL}}stylesheets/blocks.css'/>
    <link rel='stylesheet' href='{{HOME_URL}}stylesheets/slideshow.css'/>
    <link rel='icon' href='{{HOME_URL}}images/{{navigation.logo}}'>
    {% if photoshoot %}
    {% if photoshoot.chooseForEditing %}
      <link rel='stylesheet' href='{{HOME_URL}}stylesheets/chooseForEditing.css'/>
    {% endif %}
      <link rel='stylesheet' href='{{HOME_URL}}stylesheets/{{photoshoot.style}}'/>
    {% endif %}
    <title>{% if page %} {{page.name}} | {% elseif subtitle %} {{subtitle}} | {% elseif photoshoot %} {{photoshoot.name}} | {% elseif portfolio %} {% if portfolio.name %}{{portfolio.name}} |{% endif %} {% elseif decoration %}{{decoration.name}} | {% endif %} {{ title }}</title>
  </head>
  <body>
    {% include 'navigation.twig' with {current, navigation, HOME_URL} %}
    <div class="main">
      <div>
      {% block body %}{% endblock %}
      </div>
      <footer> {{footer}} </footer>
    </div>
  </body>
</html>
